[tool.mypy]
python_version = "3.10"
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.isort]
profile = "black"
line_length = 100

[tool.black]
line-length = 100

[tool.setuptools]
py-modules = ['core', 'workers', 'extract', 'database', 'scraping', 'monitoring', 'infrastructure']
